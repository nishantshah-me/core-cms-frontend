import JobCreateEditForm from 'src/components/job/JobCreateEditForm';

// export const metadata = {
//   title: `Create Job | Dashboard - ${CONFIG.appName}`,
// };

export default function CreateJobPage() {
  return <JobCreateEditForm />;
}
